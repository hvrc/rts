# Copy to backend/.env and fill in. .env is git-ignored.
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the Flask port (default 5000). On macOS, AirPlay Receiver
# often occupies 5000 — set PORT=5001 and point the frontend dev API_URL there.
# PORT=5001
